{{#each post}}
  <div class="section-wrap">
    <div class="section">
        <div class="section-title">{{ title }}</div>
            <a class="section-content" href="/post/{{ _id }}">
               by: {{ author }}
            </a>
    </div>
  </div>
{{/each}}
   

